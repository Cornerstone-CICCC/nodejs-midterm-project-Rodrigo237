Frontend (Astro)

Instrucciones rápidas:

1. Ir a la carpeta `frontend`:

   ```powershell
   cd 'c:\Users\rodri\OneDrive\Escritorio\Web_Development\Midterm_NodeJS\nodejs-midterm-project-Rodrigo237\frontend'
   ```

2. Instalar dependencias:

   ```powershell
   npm install
   ```

3. Ejecutar en modo desarrollo:

   ```powershell
   npm run dev
   ```

Notas:
- Ya existe un backend en `../backend`. Por defecto el frontend asume que el backend corre en `http://localhost:4000`.
- Si TypeScript muestra un error sobre `.astro/types.d.ts` faltante, ya incluimos un placeholder en `.astro/types.d.ts`.
